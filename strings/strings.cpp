#include <cstring>
#include <iostream>

using namespace std;

int main() {
    // cout << strlen("Hello");

    // char destination[] = "Good";

    // char source[] = "Morning";

    // strcat(destination, source);
    // cout << destination << endl;
    // strncat(destination, source, 3);
    // cout << destination << endl;

    // strcpy(destination, source);
    // cout << destination << endl;

    // strncpy(destination, source, 4);
    // cout << destination << endl;

    // char s1[] = "Programming";

    // char s2[] = "gram";

    // cout << strstr(s1, s2) << endl;

    // check for s2 in s1
    // strstr(s1, s2) ? cout << strstr(s1, s2) : cout << "Not found";

    // check occurence of letter
    // cout << strchr(s1, 'a') << endl;
    // cout << strrchr(s1, 'g') << endl;

    // compare two strings according to their position in a dict
    // for strcmp(s1,s2)
    // s1 < s2 => -1
    // s1 == s2 => 0
    // s1 > s2 => 1

    // cout << strcmp("banana", "banana") << endl;

    // string to long, string to float
    // cout << strtol("237", NULL, 10) << endl;
    // cout << strtof("29.39", NULL) << endl;

    // char s1[20] = "x=10;y=20;z=30";
    // cout << strtok(s1, "=;") << endl;

    // char *token = strtok(s1, ";");
    // while (token) {
    //     cout << token << endl;
    //     token = strtok(NULL, ";");
    // }

    return 0;
}